- name: Demo tags
  hosts: localhost
  connection: local
  tasks:
    - name: Install Nginx
      ansible.builtin.debug:
        msg: Installed Nginx
      tags:
        - install

    - name: Start Nginx
      ansible.builtin.debug:
        msg: Started Nginx
      tasks:
        - service
        - systemd
